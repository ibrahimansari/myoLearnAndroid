<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ibrahim.ansari.LearnMotion">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        android:name="ibrahim.ansari.myoLearn.myoLearn>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MyoSession"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".MyoActivity"
            android:label="@string/app_name"></activity>
        <activity android:name=".PebbleActivity"></activity>
        <activity android:name=".PebbleSession">
        </activity>
    </application>

</manifest>
